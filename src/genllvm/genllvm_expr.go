package genllvm

import (
	"fmt"
	//"strings"
	//"unicode"

	"trivil/ast"
	//"trivil/lexer"
)

var _ = fmt.Printf

func (genc *genContext) genExpr(expr ast.Expr) string {
	return "";
}

func encodeLiteralString(runes []rune) string {
	return "";
}